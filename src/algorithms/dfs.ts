import type GridNode from "../models/GridNode";

export default function dfs(grid: GridNode[][], starting_node: GridNode, endingNode: GridNode): GridNode[] {
    const visitingOrder = [];
    function ƒ(current: GridNode) {

    }
    return
}
